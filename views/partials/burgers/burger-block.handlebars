<li>
	{{burger_name}}

	<button class="futureDevoured" data-id="{{id}}" data-newdevoured="{{devoured}}">
		{{#if devoured}}Devoured it!!{{else}}New Burger{{/if}}
	</button>

	{{!-- <button class="delete-cat" data-id="{{this.id}}">DELETE!</button> --}}
</li>